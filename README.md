# Servicentro Backend (NestJS + TypeORM + PostgreSQL)

## Requisitos
- Node 18+
- PostgreSQL 14+ (o superior)

## Configuración
1. Copia `.env.example` a `.env` y ajusta credenciales.
2. Instala dependencias:
   ```bash
   npm install
